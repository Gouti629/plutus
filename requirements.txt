anthropic>=0.40.0
pydantic>=2.7
networkx>=3.0
fastapi>=0.111
uvicorn>=0.30
python-dotenv>=1.0
httpx>=0.27

# Only needed to run benchmark/vllm_bench.py against a self-hosted vLLM
# server - not required for the agent, Atlas, dashboard, or api_bench.py.
openai>=1.30
